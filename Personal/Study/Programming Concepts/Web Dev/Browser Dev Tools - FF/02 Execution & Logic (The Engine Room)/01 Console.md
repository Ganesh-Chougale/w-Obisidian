# Console  
The interactive command line for the browser. It displays Javascript errors, warnings, and custom `console.log()` statements. You can also execute arbitrary JavaScript here directly against the running page.  
