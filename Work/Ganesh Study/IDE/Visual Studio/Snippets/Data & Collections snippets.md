# Visual Studio snippets  
## Data & Collections snippets
1. Creates a basic container to group related classes together.
`namespace` + `tab` :
```csharp
namespace MyNamespace
{

}
```  

2. Creates a basic class definition shell.
`class` + `tab` :
```csharp
class MyClass
{

}
```  

3. Creates a blueprint outline for class contracts.
`interface` + `tab` :
```csharp
interface IMyInterface
{

}
```  

4. Creates a value-type data layout structure.
`struct` + `tab` :

```csharp
struct MyStruct
{

}
```  

5. Creates a set of named integer constants.
`enum` + `tab` :
```csharp
enum MyEnum
{

}
```  

6. Creates a block that cleans up disposable memory resources immediately.
`using` + `tab` :
```csharp
using (resource)
{

}
```  