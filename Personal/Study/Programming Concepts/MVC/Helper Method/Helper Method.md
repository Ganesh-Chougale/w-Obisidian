## `Helper Method`  
When we Create some methods like `NextMastCode` or `NextAmendNo`.  
we don't implement these methods as methods as full or solid methods.  
these methods are `Helper Methods`. we implement these methods within other solid methods.  
#### `Example`  
- we implement `NextAmendNo()` method in `Create()` method.  