# 1. check what you already have
```cmd
winget --version
g++ --version
gcc --version
cmake --version
qtpaths --version
qmake --version
```

# 2. install in this flow  
```
1. C++ compiler
      ↓
2. CMake
      ↓
3. Qt
      ↓
4. Qt Creator (IDE)
      ↓
5. Verify everything
```



