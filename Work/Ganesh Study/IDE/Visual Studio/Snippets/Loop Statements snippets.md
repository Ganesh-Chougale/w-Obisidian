# Visual Studio snippets  
## Loop Statements snippets

1. Creates a standard index-controlled loop.
`for` + `tab` :
```csharp
for (int i = 0; i < length; i++)
{

}
```

2. Creates a collection-traversing loop.
`foreach` + `tab` :
```csharp
foreach (var item in collection)
{

}
```  

3. Creates a pre-test condition loop.
`while` + `tab` :
```csharp
while (true)
{

}
```  

4. Creates a post-test condition loop that runs at least once.
`do` + `tab` : 
```csharp
do
{

}while (true);
```  

5. Creates a standard reverse index-controlled loop.
`forr` + `tab` :
```csharp

for (int i = length - 1; i >= 0; i--)
{

}
```  