```csharp
        public static async Task<long> Create(CastingMouldConfigAmend objCastingMouldConfigH)
        {
            try
            {
                // MstMenu _objMstMenu = MstMenu.getMenuById(MenuId);
                // _dalCnfTranTypes = new DALCnfTranTypes();
                _dalTrnCastingMouldConfigH = new DALTrnCastingMouldConfigH();

                DTOCnfTranTypes _dtoCnfTranTypes = _dalCnfTranTypes.GetExisting(TranType, TranSubType);
                Int64 intTrnNo = objCastingMouldConfigH.TrnNo;
                int intAmendNo = _dalTrnCastingMouldConfigH.GetNextAmend(intTrnNo);
                string strTranDate = DateUtility.getFormatedDate(objCastingMouldConfigH.TrnDate, 1);

                string strFinancialYear = FinancialYear.getFinancialYearByDate(strTranDate);
                objCastingMouldConfigH.CloseAmendNo = objCastingMouldConfigH.AmendNo;

                int intStatusCode = 0;
                if (Convert.ToBoolean(_objMstMenu.ApprovalReq) == true)
                {
                    intStatusCode = 101;
                }

                //if (_dtoCnfTranTypes != null)
                //{
                //    if (Convert.ToBoolean(_dtoCnfTranTypes.MONTHLY_NUMBERING) == true)
                //    {
                //        intTrnNo = _dalTrnCastingMouldConfigH.GetMonthlyTranNo(TranType, _dtoCnfTranTypes.TRAN_SERIES, strTranDate.Substring(0, 6));
                //        if (intTrnNo == 1)
                //        {
                //            intTrnNo = CustomTranNo.GetCustomMonthlyTranNo(TranType, _dtoCnfTranTypes.TRAN_SERIES, strTranDate);
                //        }
                //    }
                //    else
                //    {
                //        intTrnNo = _dalTrnCastingMouldConfigH.GetTranNo(TranType, _dtoCnfTranTypes.TRAN_SERIES, strFinancialYear);
                //        if (intTrnNo == 1)
                //        {
                //            intTrnNo = CustomTranNo.GetCustomTranNo(TranType, _dtoCnfTranTypes.TRAN_SERIES, strTranDate);
                //        }
                //    }
                //}

                objCastingMouldConfigH.DocType = _objMstMenu.VouType;
                objCastingMouldConfigH.TrnDate = strTranDate;
                objCastingMouldConfigH.TrnNo = intTrnNo;
                objCastingMouldConfigH.TrnSubType = TranSubType;
                objCastingMouldConfigH.StatusCode = intStatusCode;
                objCastingMouldConfigH.AmendNo = intAmendNo;

                long result = 0;
                result = await Task.Run(() => { return insert(objCastingMouldConfigH, 1); });
                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
```  